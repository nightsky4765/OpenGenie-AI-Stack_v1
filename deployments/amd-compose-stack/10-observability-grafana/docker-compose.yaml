services:
  prometheus:
    image: ${PROMETHEUS_IMAGE:-prom/prometheus:latest}
    container_name: prometheus
    restart: always
    volumes:
      - ./prometheus/prometheus-amd.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - ai_stack_net
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"

  gpu-exporter:
    image: ${ROCM_EXPORTER_IMAGE:-rocm/device-metrics-exporter:v1.4.2}
    container_name: gpu-exporter
    restart: always
    privileged: true
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    volumes:
      - /sys:/sys:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - ai_stack_net
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"

  cadvisor:
    image: ${CADVISOR_IMAGE:-gcr.io/cadvisor/cadvisor:latest}
    container_name: cadvisor
    restart: always
    privileged: true
    ports:
      - "8088:8080"
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - ai_stack_net
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"

  loki:
    image: ${LOKI_IMAGE:-grafana/loki:latest}
    container_name: loki
    restart: always
    ports:
      - "3100:3100"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - ai_stack_net
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"

  grafana:
    image: ${GRAFANA_IMAGE:-grafana/grafana:latest}
    container_name: grafana
    restart: always
    ports:
      - "${GRAFANA_PORT:-3000}:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASS:-CHANGE_ME}
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - ai_stack_net
    depends_on:
      - prometheus
      - loki
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"

  node-exporter:
    image: ${NODE_EXPORTER_IMAGE:-prom/node-exporter:latest}
    container_name: node-exporter
    restart: always
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
      - /var/lib/node_exporter/textfile_collector:/host/textfile:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--collector.textfile.directory=/host/textfile'
      - '--collector.filesystem.ignored-mount-points'
      - "^/(sys|proc|dev|host|etc)($$|/)"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - ai_stack_net
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"

networks:
  ai_stack_net:
    external: true

volumes:
  prometheus_data:
  grafana_data:
